alias b := build
alias r := run
alias d := disassembly

build:
    mkdir -p ./build
    gcc -g -march=native ./src/*.c -o ./build/main

run:
    ./build/main

disassembly:
    for file in ./src/*.c; do \
        target="$(basename "$file")"; \
        gcc -march=native -S ./src/$target -o ./build/${target::-2}.s; \
	done
